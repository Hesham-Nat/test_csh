CREATE OR REPLACE INTERFACE VIEW f_client (
        client_id:text,
        name:text,
        surname:text,
        client_type:text
    )
    SET IMPLEMENTATION client(
        client_id = client_id,
        name = name,
        surname = surname,
        client_type = client_type    
);

